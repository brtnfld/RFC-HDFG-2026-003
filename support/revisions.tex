\revision{v1}{July 15, 2026}{Initial draft, split out from RFC-HDFG-2026-001, rebased on that RFC's v5 pline v3 format.}
\revision{v2}{August 18, 2026}{Rebased the blob locator onto RFC-HDFG-2026-001's extensible pipeline-message encoding: the locator is now the critical extension block \texttt{H5O\_PLINE\_EXT\_BLOB} (0x0003) rather than a fixed \texttt{has\_aux}/\texttt{aux\_loc} field pair.  This RFC no longer claims a pipeline-message version, no longer requires a version~4, and is decoupled from the release that ships version~3.  Libver gating for the blob block is specified as a hard error rather than a silent downgrade.}
